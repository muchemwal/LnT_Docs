---
title: API Reference
---

import { Callout } from 'nextra/components'
import dynamic from 'next/dynamic'

export const SwaggerUI = dynamic(() => import('../components/SwaggerUI'), { ssr: false })

# API Reference

<Callout type="info">
  All endpoints require `Authorization: Bearer JWT` issued by Keycloak.
  Gateway: `http://localhost:8000` (development). PHI fields are AES-256 encrypted at rest.
</Callout>

Use the filter box below to search endpoints by tag or path. Expand any operation to see request/response schemas, parameters, and example payloads.

<SwaggerUI />
